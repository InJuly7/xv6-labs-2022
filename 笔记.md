
### Q1: 从 write 到 usertrap
**usys.S 汇编**
- 将系统调用号放入`$a7`寄存器
    - a0 = 文件描述符 (如stdout的1)
    - a7 = 16 (SYS_write)
    - pc = write函数中ecall指令地址
- 执行系统调用指令(ecall)
- 返回应用程序

**ecall指令**

- 从用户态(U-mode)切换到监管态(S-mode)
- pc + 4 --> `$spec`
- `$stvec` --> pc (uservec)
    - 0x3f ffff f000
- 跳转到陷阱处理

**进入 uservec (处于监管者模式, 仍使用用户页表)**
- `$a0` <--> `$sscratch` 
- 设置 trapframe 地址 TRAPFRAME-> `$a0`
    - TRAPFRAME是一个固定的虚拟地址（通常是0x3ffffffe000）每个进程的trapframe物理地址不同，但虚拟地址相同
- 保存所有用户寄存器到 trapframe (user address)
    - 如 `$ra` `$sp`
    - 每个寄存器按照固定偏移量保存到trapframe中
- p->trapframe->ra -> `$ra` (保存返回地址)
- p->trapframe->kernel_sp -> `$sp` ($sp寄存器保存初始化内核指针)
    - 0x 3f ffff 9000
- p->trapframe->kernel_hartid -> `$tp` ($tp寄存器保存当前CPU核心的ID)
    - 0x0
- p->trapframe->kernel_trap -> `$t0` ($t0寄存器保存usertrap()函数入口)
    - 0x 8000 1c86
- p->trapframe->kernel_satp -> `$t1` ($t1寄存器保存内核页表地址)
    - 内存屏障 sfence.vma确保内存操作的顺序, 刷新tlb
- `$stap` <--> `$t1` (安装内核页表, 用户页表存储在`$t1`)
    - 0x 8000 0000 0008 7fff
- 刷新TLB中现在过时的用户条目
- 跳转到 `$t0`


### lab3 Speed up system calls 
#### 实验流程
创建一个内核与用户共享的页面, 该页面的内核地址空间与用户地址空间 指向同一个物理页面
将系统调用的返回值写入共享页面(用户只读, 内核可读写),减少内核切换的开销.
- 定义一个用户空间只读共享页(TRAPFRAME - PGSIZE -> USYSCALL)
    - 将 struct usyscall 存储在共享页的开始处
- USYSCALL(虚拟页面) 映射到 物理页面
    - 在 创建这个进程的时候 将物理共享页面分配出来, 写入 pid 号
    - 在页表中插入PTE页表项(USYSCALL -> 共享物理页面)
- 内核为每个进程创建一个共享页
- 处理释放页面 释放进程

#### 数据结构与算法分析
1. 进程初始化入口 : `proc.c --> static struct proc *allocproc(void)`  
    - 从进程表中 找到一个进程状态为 `UNUSED` 的进程
    - 分配 `pid` 
    - 初始化 `state`
    - 分配 `trapframe` 陷阱帧
    - 分配 共享物理页 (返回地址转化为 `struct usyscall *`)
    - 共享物理页面 存储 `pid` 号
    - 建立 映射的页表(`proc_pagetable`)
    - 初始化 寄存器状态

2. 建立映射的页表项 : `proc.c --> pagetable_t proc_pagetable(struct proc *p)`
    - 分配一个空页表
    - 建立 `TRAMPOLINE` `TRAPFRAME` `USYSCALL` 三个虚拟页面的映射 `mappages`

3. 页面映射接口 : `vm.c --> int mappages(pagetable_t pagetable, uint64 va, uint64 size, uint64 pa, int perm)`
    - 将地址 向上,下对齐到页边界
    - 通过`walk`接口 获得一级页表对应的 pte 地址
    - 配置符号位
4. 释放物理页入口 : `static void freeproc(struct proc *p)`
    - 释放用户进程页表(解映射)


```cpp
// 数据结构
struct proc proc[NPROC] // 进程表
struct proc {
    struct spinlock lock; // 锁
    // p->lock must be held when using these:
    enum procstate state; // Process state
    ... ... ...
    pagetable_t pagetable;       // User page table
    struct trapframe *trapframe; // data page for trampoline.S
    struct usyscall *usyscall;   // 共享页面
}
enum procstate { UNUSED, USED, SLEEPING, RUNNABLE, RUNNING, ZOMBIE };

typedef uint64 *pagetable_t; // 512 PTEs
#define USYSCALL (TRAPFRAME - PGSIZE) // 定义用户空间的共享虚拟页
```

#### 测试
如果在运行 pgtbltest 时， ugetpid 测试用例通过，你将获得此实验部分的全部分数。



### lab3 Print a page table 
#### 实验流程
任务 : 编写一个函数，打印页表的内容, 打印 xv6 第一个进程页表

- xv6启动进程入口(initcode.S:exec)
    - 校验 pid 是否为1
- 编写 打印 vmprint 函数

#### 数据结构与算法分析
1. 函数声明 `defs.h --> void vmprint(pagetable_t pagetable, int level);` 函数定义 `vm.c`
2. xv6启动 init进程入口
    - initcode.S:start -> exec 校验 pid 是否为1
3. 函数实现 递归打印有效页表项

```cpp
// 数据结构
pagetable_t pagetable // 用户进程页表
int level // 从三级页表开始
```

#### 测试
如果你通过了 pte printout 测试，来自 make grade ，你将获得这部分实验的全部积分。


### lab3 Detect which pages have been accessed  

一些垃圾回收器（自动内存管理的一种形式）可以通过获取哪些页面已被访问（读或写）的信息来提高效率。
在本实验中，您将为 xv6 添加一个新功能，通过检查 RISC-V 页表中的访问位来检测并报告这些信息给用户空间。
每当 RISC-V 硬件页表遍历器解决 TLB 未命中时，它会在 PTE 中标记这些位。

你的任务是实现 pgaccess() ，这是一个系统调用，用于报告哪些页面已被访问。
该系统调用接受三个参数。
- 首先，它接受要检查的第一个用户页面的起始虚拟地址。
- 其次，它接受要检查的页面数量。
- 最后，它接受一个指向缓冲区的用户地址，
    - 将结果存储到位掩码中（该数据结构每个页面使用一个位，且第一个页面对应最低有效位）。
#### 实验流程
- 阅读 pgtbltest.c 代码
- 注册 pgacess() 系统调用
- 接收 base, len, mask 三个参数
- 配置 `PTE` 中某一位 是访问位 
- 通过 `struct proc` 访问 进程页表 
- 访问每个页面的 一级页表项(walk) 以及 符号位
    -  赋值给掩码 并 置0
- 将数据写回用户空间



#### 数据结构与算法分析
- 系统调用注册 见lab2
- 系统调用声明,定义
    - `sysproc.c --> int sys_pgaccess(void)`
    - `defs.h, proc.c --> int pgaccess(void *base, int len, void *mask);`
- 配置访问位 `#define PTE_A (1L << 6) // 访问位`
- 系统调用实现
    - 内核态 获取系统调用参数
    - 获取 连续 len 大小 页面访问位
        - 计算页面虚拟地址, 通过(walk) 用户页表映射, 获取一级页表项
        - 校验 `PTE_A` 刷新 `temp_mask`
    - 通过 `copyout` 复制回 用户空间

```cpp
// 数据结构 
uint64 base; // 访问的虚拟地址(用户地址空间)
int len; // 访问大小
uint64 mask; // 掩码地址 (最多可以判断连续的32个页, 是否被访问过)(用户地址空间)

struct proc *p // 当前用户进程
p->pagetable // 用户页表
uint32 temp_mask = 0; // (内核地址空间)
```

#### 测试
如果在运行 pgtbltest 时 pgaccess 测试用例通过，你将获得实验部分的满分。




### xv6 启动到 到第一个 `$` 输出
- 启动进程
    - entry.S:_entry -> start.c:start -> main.c:main -> proc.c:scheduler -> swtch.S:swtch
- init进程
    - initcode.S:start -> exec ->  init.c:main -> fork
- sh 进程(init 子进程)
    - fork -> exec -> sh

