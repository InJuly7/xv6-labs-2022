### 调用者寄存器, 被调用者寄存器：

1. **被调用者寄存器(sp, s0-s11)**：在各自函数的开头保存，结尾恢复
2. **调用者寄存器(ra, t0-t6, a0-a7)**：由调用方选择性保存
3. **栈参数**：超过8个的参数放在栈上，被调用方通过偏移访问
4. **栈帧嵌套**：每个函数都有自己的栈帧，形成嵌套结构


```js
函数调用链：C → A → B

═══════════════════════════════════════════════════════════════════

【初始状态 - 在函数C中】
┌─────────────────┐  
│   C的局部变量    │
│   C的保存寄存器   │
├─────────────────┤ ← sp (C的栈帧)
│      ...        │
└─────────────────┘

═══════════════════════════════════════════════════════════════════

【函数C调用函数A】

C中的代码：
```assembly
func_c:
    # C如果需要在调用A后继续使用t0,t1，要自己保存
    sd t0, -8(sp)      # 保存caller-saved寄存器(可选)
    sd t1, -16(sp)     
    addi sp, sp, -16
    
    li a0, 10          # 准备参数
    li a1, 20          
    call func_a        # ra = 返回到C的地址
```

调用后栈状态：
┌─────────────────┐  
│   C的局部变量    │
│   C的保存寄存器   │
├─────────────────┤
│    t1 (C保存)   │  ← C选择保存的caller-saved
│    t0 (C保存)   │
├─────────────────┤  ← sp
│                 │
│   (A的栈空间)    │
└─────────────────┘

寄存器状态：
ra = 返回到C的地址
a0 = 10, a1 = 20 (参数)

═══════════════════════════════════════════════════════════════════

【进入函数A - A的Prologue】

```assembly
func_a:
    # A立即保存自己需要的callee-saved寄存器
    addi sp, sp, -32
    sd ra, 24(sp)      # 保存返回到C的地址
    sd s0, 16(sp)      # 保存callee-saved寄存器  
    sd s1, 8(sp)       # 保存callee-saved寄存器
    sd s2, 0(sp)       # 保存callee-saved寄存器
```

A的栈帧建立后：
┌─────────────────┐  
│   C的局部变量    │
│   C的保存寄存器   │
├─────────────────┤
│    t1 (C保存)   │  
│    t0 (C保存)   │
├─────────────────┤  
│ ra→C (A保存)    │  ← A保存的返回地址
│    s0 (A保存)   │  ← A保存的callee-saved
│    s1 (A保存)   │  
│    s2 (A保存)   │  
├─────────────────┤  ← sp (A当前栈顶)
│                 │
│  A的工作空间     │
└─────────────────┘

寄存器状态：
ra = 返回到C的地址 (已保存到栈中)
a0 = 10, a1 = 20 (参数，A可以直接使用或修改)
s0, s1, s2 = 原值已保存，A可以随意使用

═══════════════════════════════════════════════════════════════════

【函数A调用函数B】

A中的代码：
```assembly
    # A准备调用B，需要传递更多参数(假设超过8个)
    li a0, 1           # 参数1
    li a1, 2           # 参数2
    # ... a0-a7 (前8个参数)
    
    # 第9、10个参数要放栈上
    sd t0, -8(sp)      # 第9个参数
    sd t1, -16(sp)     # 第10个参数
    addi sp, sp, -16
    
    call func_b        # ra = 返回到A的地址
```

调用B前的栈状态：
┌─────────────────┐  
│   C的局部变量    │
├─────────────────┤
│    t1 (C保存)   │  
│    t0 (C保存)   │
├─────────────────┤  
│ ra→C (A保存)    │  
│    s0 (A保存)   │  
│    s1 (A保存)   │  
│    s2 (A保存)   │  
├─────────────────┤  
│   参数10 (A传递) │  ← A传递给B的栈参数
│   参数9 (A传递)  │  
├─────────────────┤  ← sp
│                 │
│   (B的栈空间)    │
└─────────────────┘

寄存器状态：
ra = 返回到A的地址
a0-a7 = 前8个参数

═══════════════════════════════════════════════════════════════════

【进入函数B - B的Prologue】

```assembly
func_b:
    # B立即保存自己需要的callee-saved寄存器
    addi sp, sp, -24
    sd ra, 16(sp)      # 保存返回到A的地址
    sd s0, 8(sp)       # B需要使用s0
    sd s1, 0(sp)       # B需要使用s1
```

B的完整栈帧：
┌─────────────────┐  
│   C的局部变量    │
├─────────────────┤
│    t1 (C保存)   │  
│    t0 (C保存)   │
├─────────────────┤  
│ ra→C (A保存)    │  ← A的栈帧
│    s0 (A保存)   │  
│    s1 (A保存)   │  
│    s2 (A保存)   │  
├─────────────────┤  
│   参数10        │  ← B可以通过32(sp)访问
│   参数9         │  ← B可以通过24(sp)访问
├─────────────────┤  
│ ra→A (B保存)    │  ← B保存的返回地址
│    s0 (B保存)   │  ← B保存的callee-saved  
│    s1 (B保存)   │  
├─────────────────┤  ← sp (B当前栈顶)
│                 │
│  B的工作空间     │
└─────────────────┘

寄存器状态：
ra = 返回到A的地址 (已保存)
a0-a7 = 参数1-8 (B可以直接使用)
s0, s1 = 原值已保存，B可以随意使用

═══════════════════════════════════════════════════════════════════

【返回过程】

B返回到A：
```assembly
func_b:
    # B的Epilogue
    ld ra, 16(sp)      # 恢复返回到A的地址
    ld s0, 8(sp)       # 恢复s0原值
    ld s1, 0(sp)       # 恢复s1原值  
    addi sp, sp, 24
    ret                # 跳转到A
```

A继续执行，然后返回到C：
```assembly
func_a:
    # A的Epilogue
    addi sp, sp, 16    # 释放传递给B的栈参数空间
    ld ra, 24(sp)      # 恢复返回到C的地址
    ld s0, 16(sp)      # 恢复s0原值
    ld s1, 8(sp)       # 恢复s1原值
    ld s2, 0(sp)       # 恢复s2原值
    addi sp, sp, 32
    ret                # 跳转到C
```


### lab4 traps
111
